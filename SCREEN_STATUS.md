# 화면 구현 상태

> 작업 시작 전/완료 후 갱신. 매치 실행 없이 코드 기반 리뷰 시 SoT(Single Source of Truth).
> 상태 마커는 `CLAUDE.md` "화면 구현 상태 추적 규칙" 참고.

## 라우트별 상태

| 라우트              | 상태 | 파일                                                                                                                                                                 | 설명                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| ------------------- | ---- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `/`                 | ✅   | `app/page.tsx` + `app/_components/HomeClient.tsx`                                                                                                                    | 서버 wrapper + HomeClient (**글로벌 네비 메뉴바(GlobalNav) + UserMenu 헤더** + 로그인/비로그인 분기 + 대전하기 카드(→/dashboard) + 리더보드 카드(→/leaderboard) **2열** 활성화, PR #7-C/#7-D + 화면 개선 + MVP A-1/A-2). **2026-06-06: 매치 찾기 카드 제거(대시보드 "자동 매칭"과 기능 중복) → 카드 2개 `md:grid-cols-2`, MatchmakingDialog/관련 state·import dead code 정리. 헤더에 GlobalNav(홈/대전하기/리더보드, `usePathname` active 강조) 추가**                                                                                                                                                                                                                                                                                                                                                                  |
| `/play/[matchId]`   | ✅   | `app/play/[matchId]/page.tsx` + `_components/HostWaitingView.tsx` + `_components/WaitingForGameStart.tsx`                                                            | middleware SSR 가드 + 5단계 분기 (loading / matchStatusError / matchStatus null / waiting+host(HostWaitingView) / waiting+게스트(WaitingForGameStart) / ongoing\|finished 게임 화면), `useMatchStatus` 훅으로 matches 실시간 동기화 (postgres_changes + 30초 polling fallback). **글로벌 헤더 PR(B)에서 `(main)` 그룹 밖으로 이전 — Monaco 풀스크린 보존을 위해 글로벌 헤더 미적용** (PR #7-C/#7-D + B PR). **2026-06-06: 좌측 문제 패널(`ProblemPanel`)을 섹션 카드로 재구성 — `problems` 구조화 컬럼(description/input_format/output_format/examples) 직접 사용, `ProblemMetaHeader`(메타) + `ProblemSection`(📝/📥/📤 카드, 마크다운 본문) + `ProblemExampleSection`(예시 입출력 `<pre>` 카드 + explanation). 마크다운 파싱 방식 폐기**                                                                              |
| `/invite/[token]`   | ✅   | `app/invite/[token]/page.tsx` + `_components/JoinInvite.tsx`                                                                                                         | 비인증 허용 서버 컴포넌트 + 5분기 검증 (`not_found` / `already_started` / `already_finished` / `expired` / `full`). JoinInvite 클라이언트 — 호스트 본인 자기 redirect / 비로그인 시 `/login?next=...` CTA / 게스트 입장 버튼 (PR #7-D)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| `/result/[matchId]` | ✅   | `app/(main)/result/[matchId]/page.tsx` + `_components/{ResultView,ResultHeader,ParticipantCodeCard,AiReviewSection}.tsx` + `_utils/{getResultData,highlightCode}.ts` | Step 4-A(결과) + **Step 4-B(AI 리뷰, PR #20)** 완료 — server component + status pre-check + Promise.all 풀 fetch + Shiki SSR + RLS 자연 게이트. `/play` finished 배너 Link로 진입. **AI 리뷰: 본인 `ai_reviews` SSR 조회(캐싱 히트 즉시 표시) → 없으면 `AiReviewSection`(client)이 `POST /api/match/[matchId]/review` lazy 호출 → 복잡도/강점/개선/상대비교 표시.** **Step 4.5에서 MMR 변동 정적 텍스트 표시 추가(`ParticipantCodeCard`에 `+24 · Silver 1224` 형태, mmr_change NULL이면 숨김).**                                                                                                                                                                                                                                                                                                                        |
| `/login`            | ✅   | `app/(auth)/login/page.tsx` + `_components/OAuthButton.tsx` + `_utils/buildOAuthRedirect.ts` + `(auth)/layout.tsx`                                                   | OAuth(Google/GitHub) 버튼 + 정식 계정 자동 redirect (PR #7-B, 게스트 플로우는 `feature/remove-guest-flow`에서 제거)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| `/auth/callback`    | ✅   | `app/auth/callback/route.ts` (라우트 그룹 밖, runtime=nodejs)                                                                                                        | `exchangeCodeForSession` + `profiles.update`로 닉네임/아바타 동기화 (1·2단 fallback) + `?next` 또는 `/`로 redirect (PR #7-B)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| `/dashboard`        | ✅   | `app/(main)/dashboard/page.tsx` + `_components/InviteCard.tsx`                                                                                                       | 친구 초대 카드 — `POST /api/match/invite` 호출 → Dialog 모달에 inviteUrl 노출 + 클립보드 복사 + "방으로 입장" CTA. **B PR에서 임시 헤더 + 외곽 wrapper 제거 → `(main)/layout.tsx` 글로벌 헤더로 위임** (PR #7-D + B PR)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| `/leaderboard`      | ✅   | `app/(main)/leaderboard/page.tsx` + `_components/LeaderboardView.tsx` + `app/features/leaderboard/{types,utils/getLeaderboard}`                                      | MVP A-1 — server component. `getLeaderboard`로 `profiles`를 MMR DESC(→created*at ASC) 정렬 select(RLS 전체 read, 익명 `Anon*`유저 제외).`<ol>/<li>`순위 리스트, 각 행`Link`로 프로필 이동(base-ui onClick 회피), 본인 행 ring+"나" 배지, tier/mmr 배지는 `getTierByMmr`파생. **전적 표시: 닉네임 아래`7승 4패 · 64%`서브텍스트(승/패/무 + 승률). 승률 = wins/total_finished(무승부 포함 분모), 0전이면 "전적 없음".`get_leaderboard` SECURITY DEFINER RPC로 전체 유저 mmr+전적 집계(2026-06-13, Post-MVP A-1 후속 완료)\*\*. 진입: 글로벌 헤더 링크 + 홈 카드 (MVP A-1)                                                                                                                                                                                                                                                 |
| `/profile/[userId]` | ✅   | `app/(main)/profile/[userId]/page.tsx` + `_components/ProfileView.tsx` + `ProfileEditDialog.tsx` + `NicknameFallbackDialog.tsx`                                      | server component — `Promise.all`로 profile + `get_profile_stats` RPC 병렬 fetch + isMe 분기. ProfileView (Avatar/닉네임/**MMR·tier 배지**/가입일/bio/누적 전적) + 본인이면 ProfileEditDialog + 자동 생성 닉네임이면 NicknameFallbackDialog 자동 발화. middleware `/profile` prefix 가드로 비로그인 차단. **MMR·tier 배지는 `profiles.mmr` select + `getTierByMmr` 파생(결과 페이지와 동일 산출), mmr NULL이면 1000 fallback→Bronze** (PR #18 + 화면 개선). **대전 히스토리 섹션 추가(2026-06-13, A-5): 누적 전적 아래 개별 매치 리스트 — 승/패/무 배지 + 상대 아바타/닉네임 + 문제 + 날짜 + MMR변동(NULL 숨김), 행 전체 클릭→`/result/[matchId]`. `get_match_history` SECURITY DEFINER RPC(RLS 우회, authenticated 단독)로 최근 20건, 익명(`Anon_`) 상대는 "익명 상대" 라벨 + 링크 비활성. 본인·타인 프로필 모두 노출** |
| `/profile/me`       | ✅   | `app/(main)/profile/me/page.tsx`                                                                                                                                     | server component — `auth.getUser()` → `redirect(/profile/${user.id})`. middleware 가드 + 라우트 자체 방어 redirect (PR #18)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |

## 인증 / 공통 인프라

| 영역                                          | 상태 | 위치 / 파일                                                                   | 설명                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| --------------------------------------------- | ---- | ----------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| (main) 글로벌 헤더 layout                     | ✅   | `app/(main)/layout.tsx`                                                       | server component, sticky 헤더 (`h-14`, `z-40`, `backdrop-blur`), 좌측 **`<GlobalNav />`(홈/대전하기/리더보드 메뉴바, 2026-06-06 — 기존 "리더보드" 단일 링크 대체)** + 우측 `<UserMenu />` (justify-between). `<main flex-1>` wrapper로 자식 페이지가 풀높이 확보. `/dashboard`, `/profile/[userId]`, `/leaderboard`, `/result/[matchId]`에 자동 적용. **`/play/[matchId]`는 Monaco 풀스크린 보존을 위해 `(main)` 그룹 밖으로 이전 — 헤더 미적용** (B PR)  |
| 통합 인증 상태 훅                             | ✅   | `app/shared/hooks/useAuth.ts`                                                 | React Query 단일 진입점. user + profiles 조회 + fallback upsert                                                                                                                                                                                                                                                                                                                                                                                           |
| Supabase anon 클라이언트                      | ✅   | `app/shared/lib/supabase/{client,server}.ts`                                  | 브라우저/서버 클라이언트 분리 (RLS 검증 경로)                                                                                                                                                                                                                                                                                                                                                                                                             |
| Supabase service 클라이언트                   | ✅   | `app/shared/lib/supabase/service.ts` (PR #8)                                  | 서버 전용 service-role. 히든 test_cases 채점 시 RLS bypass                                                                                                                                                                                                                                                                                                                                                                                                |
| middleware.ts                                 | ✅   | `middleware.ts`                                                               | 세션 쿠키 갱신 + 보호 prefix(`/play`, `/result`, `/dashboard`, `/profile`, `/leaderboard`) SSR 가드 + `/api/*` 분기 (PR #7-C + PR #18 `/profile` 일반화 + MVP A-1 `/leaderboard` 추가)                                                                                                                                                                                                                                                                    |
| OAuth 로그인 플로우                           | ✅   | `app/(auth)/login/_components/OAuthButton.tsx` + `app/auth/callback/route.ts` | `signInWithOAuth` 단일 경로 + 콜백에서 `exchangeCodeForSession` + 닉네임/아바타 동기화 (PR #7-B)                                                                                                                                                                                                                                                                                                                                                          |
| AuthListener (전역 단일)                      | ✅   | `app/shared/components/AuthListener.tsx`                                      | QueryProvider 내부 마운트. SIGNED_IN/SIGNED_OUT/USER_UPDATED만 `AUTH_QUERY_KEY` invalidate (PR #7-C)                                                                                                                                                                                                                                                                                                                                                      |
| UserMenu 드롭다운                             | ✅   | `app/shared/components/UserMenu.tsx`                                          | 로그인/비로그인 분기 + Avatar 드롭다운 + **프로필(`/profile/me`) 이동** + 로그아웃(진행 중 프로필 이동 가드). **마운트 지점 2곳**: `app/_components/HomeClient.tsx` (홈, `(main)` 밖) + `app/(main)/layout.tsx` (글로벌 헤더). 홈 헤더는 글로벌 헤더와 동일 className으로 시각 일관성 통일 (B PR + 화면 개선). **2026-06-06: 두 헤더 모두 좌측에 `<GlobalNav />` 추가 — 홈/(main) 페이지가 동일 메뉴바를 가짐 (홈 헤더 `justify-end`→`justify-between`)** |
| `GlobalNav` 메뉴바                            | ✅   | `app/shared/components/GlobalNav.tsx`                                         | **글로벌 네비 메뉴바 (2026-06-06)** — client component. `usePathname` 정확 일치 시 active(`secondary` + `aria-current="page"`), 아니면 `ghost`. 항목 3개(홈/대전하기/리더보드) `NAV_ITEMS` 상수. base-ui 제약상 `<Link className={buttonVariants(...)}>` 패턴. `nav`에 `min-w-0 overflow-x-auto`로 모바일 overflow 대응. 비로그인 무관 항상 노출. `(main)/layout.tsx` + `HomeClient.tsx` 양쪽 헤더에서 재사용                                             |
| `requireUser` API 가드                        | ✅   | `app/shared/lib/auth/requireUser.ts`                                          | 6개 API 라우트(match × 3, judge, problems × 2)에 401 가드 통일 (PR #7-C). 신규 `match/invite`도 동일 적용 (PR #7-D)                                                                                                                                                                                                                                                                                                                                       |
| `protectedPaths` 보호 prefix                  | ✅   | `app/shared/lib/auth/protectedPaths.ts`                                       | middleware + UserMenu 공유. `PROTECTED_PREFIXES` + `isProtectedPath()` (PR #7-C). PR #18에서 `/profile/me` → `/profile`로 일반화하여 `/profile/[userId]`도 자동 가드                                                                                                                                                                                                                                                                                      |
| `sanitizeNext` open redirect 차단             | ✅   | `app/(auth)/login/_utils/sanitizeNext.ts`                                     | `/login`, OAuth callback, OAuth start, middleware 4곳 same-origin 화이트리스트 (PR #7-C)                                                                                                                                                                                                                                                                                                                                                                  |
| `useMatchStatus` 훅                           | ✅   | `app/features/match/hooks/useMatchStatus.ts`                                  | 초기 fetch + Realtime postgres_changes UPDATE + 30초 polling fallback (3개 useEffect 자체 isMounted, 채널명 `match-status:${matchId}`) (PR #7-D)                                                                                                                                                                                                                                                                                                          |
| `MatchmakingDialog` 공용 모달                 | ✅   | `app/features/match/components/MatchmakingDialog.tsx`                         | **자동 매칭 대기 모달 (MVP A-2)** — 홈/대시보드 placeholder 2곳에서 재사용. open 시 join 호출 → `matched=true`면 즉시 `/play`, `false`면 "매칭 중..." 스피너 + 취소. 취소/닫기/`beforeunload` 시 leave(sendBeacon). `hasNavigatedRef`로 join 응답+Realtime 중복 push 가드                                                                                                                                                                                 |
| `useMatchmakingQueue` 훅                      | ✅   | `app/features/match/hooks/useMatchmakingQueue.ts`                             | **자기 큐 row 구독 (MVP A-2)** — Realtime postgres_changes UPDATE(`user_id=eq`) + 5초 polling fallback, `match_id` 채워지면 `onMatched`. 2개 useEffect 자체 isMounted 분리, 채널명 `matchmaking-queue:${userId}`, createClient useMemo 1회                                                                                                                                                                                                                |
| `buttonVariants` server-safe 모듈             | ✅   | `components/ui/button-variants.ts`                                            | cva 정의를 `"use client"` 없는 별도 모듈로 분리. server/client 양쪽에서 호출 가능 (PR #7-D)                                                                                                                                                                                                                                                                                                                                                               |
| `get_invite_match_by_token` RPC               | ✅   | `supabase/migrations/20260510_tighten_rls_for_invite_security.sql`            | SECURITY DEFINER STABLE, anon+authenticated EXECUTE. `/invite/[token]` 비인증 검증용. 반환 컬럼에서 `invite_token` 제외 (PR #14)                                                                                                                                                                                                                                                                                                                          |
| `service.ts` service-role 단일화              | ✅   | `app/shared/lib/supabase/service.ts`                                          | PR #16에서 `admin.ts` 제거하고 `createServiceClient()`(싱글턴 + fail-fast E_SERVICE)로 통일. `/api/match/[matchId]/join` + `submit`이 재사용                                                                                                                                                                                                                                                                                                              |
| matches/match_participants 보호 정책 + 트리거 | ✅   | `supabase/migrations/20260516_*.sql` (4개)                                    | matches: participant_update 정책은 1차로 도입 후 winner write primitive 결함으로 DROP, prevent_protected_matches_update 트리거는 service-role 우회 안전망 역할로 유지. match_participants: 동일 패턴(self_update 정책 1차 도입 후 score write primitive 결함으로 DROP, 트리거 유지). 두 테이블 모두 인가 사용자 UPDATE 는 default deny, submit/join 라우트는 service-role 클라이언트로 갱신 (PR #16)                                                      |

## API 라우트 (참고용)

| 경로                                       | 상태 | 설명                                                                                                                                                                                              |
| ------------------------------------------ | ---- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `app/api/match/invite/route.ts`            | ✅   | 친구 초대 매치 + 토큰 발급 (Node.js runtime, 충돌 재시도, PR #7-D)                                                                                                                                |
| `app/api/match/matchmaking/join/route.ts`  | ✅   | **자동 매칭 큐 진입 + 동기 매칭 (MVP A-2)** — requireUser + 본인 MMR 조회 + service-role `find_or_enqueue_match` RPC 호출. `{matched:true, matchId}` 또는 `{matched:false}` 반환 (nodejs runtime) |
| `app/api/match/matchmaking/leave/route.ts` | ✅   | **자동 매칭 큐 이탈 (MVP A-2)** — requireUser + anon self_delete DELETE (멱등, row 0 정상). sendBeacon 호환                                                                                       |
| `app/api/match/[matchId]/join/`            | ✅   | 매치 참가                                                                                                                                                                                         |
| `app/api/match/[matchId]/submit/`          | ✅   | 코드 제출 + 결과 반영                                                                                                                                                                             |
| `app/api/match/[matchId]/review/`          | ✅   | AI 코드 리뷰 생성/조회 (requireUser + 본인 submission 소유검증 + 캐싱 + Gemini + service-role upsert, nodejs runtime, PR #20)                                                                     |
| `app/api/judge/route.ts`                   | ✅   | AI 채점                                                                                                                                                                                           |
| `app/api/problems/route.ts`                | ✅   | 문제 목록                                                                                                                                                                                         |
| `app/api/problems/[problemId]/`            | ✅   | 문제 단건 조회                                                                                                                                                                                    |
| `app/api/profile/me/route.ts`              | ✅   | PATCH 본인 프로필 갱신 (nickname/bio whitelist + `requireUser` 401 + UNIQUE 23505 → 409 + RLS silent fail 가드, PR #18)                                                                           |

## DB / RLS 상태 (요약)

> 상세 검증은 `CLAUDE.md` "Supabase DB 상태 검증 규칙" 참고.
> 테이블별 row/정책 카운트는 `PROJECT_STATUS.md` "DB 상태" 섹션 참고.

- 테이블: `profiles`, `matches`, `match_participants`, `submissions`, `problems`, `test_cases`, `ai_reviews`, **`matchmaking_queue`(MVP A-2)** 모두 RLS enabled
- `matchmaking_queue` RLS (MVP A-2): `matchmaking_queue_self_read`(SELECT, TO authenticated, `user_id=auth.uid()`) + `matchmaking_queue_self_delete`(DELETE, 동일). **INSERT/UPDATE 정책 모두 부재(default deny) → `status`/`match_id` 는 service-role `find_or_enqueue_match` RPC 단독으로만 쓰여짐(write primitive 방지). self_insert 를 두지 않은 이유: 인가 사용자가 PostgREST 로 `{status:'matched', match_id:<임의>}` 직접 INSERT 위조 가능(score/winner 와 동형).** 컬럼: id/user_id(UNIQUE FK→profiles)/mmr/status(waiting·matched)/match_id(FK→matches)/created_at
- `profiles` RLS: `profiles_authenticated_read` (TO authenticated USING true — anon 차단, PR #14) + `self_update` + `self_insert` (PR #7-A 보강). **self_update 는 유지하되 Step 4.5 에서 `prevent_protected_profiles_update` 트리거로 평점 컬럼(mmr/tier/wins/losses/streak/id/created_at) OLD 고정 — nickname/bio/avatar_url 만 인가 사용자 편집 허용, mmr/tier 갱신은 service-role 단독(MMR write primitive 방지)**
- `problems` RLS: `public_read` (PR #8)
- `test_cases` RLS: `visible_read` (PR #8 — 히든은 service role 전용)
- `ai_reviews` RLS: `self_read` (SELECT, PR #8 — `submission_id IN (...)` + `TO authenticated`). **write 정책 부재 = 인가 사용자 INSERT default deny → 리뷰 저장은 service-role 단독 경로(write primitive 방지, PR #20). `submission_id` UNIQUE(`ai_reviews_submission_id_key`) 기존재**
- `matches` RLS: `matches_self_or_participant_read` (TO authenticated, `host_id = auth.uid()` OR participant — anon 차단, PR #14) / `anon_insert` / `participant_delete_waiting`. **participant_update 정책은 PR #16 1차 도입 후 winner write primitive 결함으로 같은 PR 후속 커밋에서 DROP — 인가 사용자 UPDATE 는 default deny, matches finalize 는 service-role 전용**
- `match_participants` RLS: `match_participants_co_participant_read` (TO authenticated, 자기 row OR 같은 매치 참가자 row — anon 차단, PR #14) / `self_insert` / `self_delete`. **self_update 정책은 PR #16 1차 도입 후 score write primitive 결함으로 같은 PR 후속 커밋에서 DROP — 인가 사용자 UPDATE 는 default deny, score 갱신은 service-role 전용**
- **BEFORE UPDATE 트리거** (PR #16): `prevent_protected_matches_update` (matches 7개 컬럼 OLD 고정) / `prevent_protected_match_participants_update` (match_participants 6개 컬럼 OLD 고정). **두 테이블 모두 winner/score write primitive fix 후 인가 사용자 UPDATE 정책이 부재하므로 인가 사용자 흐름은 트리거 미도달**, service-role 우회 안전망 역할. 양쪽 모두 `auth.role() = 'service_role'` 분기로 service-role 호출 우회
- **BEFORE UPDATE 트리거** (Step 4.5): `prevent_protected_profiles_update` (profiles 7개 컬럼 mmr/tier/wins/losses/streak/id/created_at OLD 고정). matches/match_participants 와 달리 **self_update 정책은 유지**(nickname/bio/avatar_url 편집용)하고 트리거가 평점 컬럼만 보호. `auth.role() = 'service_role'` 분기로 submit 라우트의 MMR 갱신은 우회. **DB 적용 완료 확인**
- **RPC** (6개):
  - `get_invite_match_by_token(p_token text)` — SECURITY DEFINER STABLE, anon+authenticated EXECUTE. `/invite/[token]` 비인증 페이지가 RLS 좁힘 후에도 토큰 검증 가능. 반환 컬럼에서 `invite_token` 제외 (PR #14)
  - `get_profile_stats(p_user_id uuid)` — SECURITY DEFINER STABLE, anon+authenticated EXECUTE. matches/match_participants RLS가 본인 외 데이터 SELECT 차단하므로 타인 프로필 진입 시 전적 0/0/0 표시 회귀를 막기 위해 도입. 반환: `{wins, losses, draws, total_finished}` (PR #18)
  - `get_leaderboard(p_limit integer)` — SECURITY DEFINER STABLE, **authenticated EXECUTE 단독**(anon 명시 REVOKE, `aclexplode` 검증 완료: postgres/service*role/authenticated만). `get_profile_stats`의 전체 유저 확장판. LEFT JOIN으로 0전 유저 포함, finished 매치 기준 mmr+wins/losses/draws/total_finished 집계, 익명(Anon*) 제외 + mmr DESC NULLS LAST → created_at ASC 정렬. 리더보드 전적 표시용 (2026-06-13, Post-MVP A-1 후속)
  - `find_or_enqueue_match(p_user_id uuid, p_mmr integer)` — SECURITY DEFINER, **service_role EXECUTE 단독**(REVOKE FROM PUBLIC). 큐 stale 정리(upsert) → 대기 상대 1명 `FOR UPDATE SKIP LOCKED` 탐색(MMR 가장 가까운 순, 10분 좀비 제외) → 있으면 매치(`ongoing`, host_id NULL) + 참가자 2명 + 상대 큐 row `matched` 갱신, 없으면 본인 큐 등록(ON CONFLICT 멱등). 반환 `(matched bool, out_match_id uuid)`. 동시 진입 race 차단 (MVP A-2)
  - `get_match_history(p_user_id uuid, p_limit integer)` — SECURITY DEFINER STABLE, **authenticated EXECUTE 단독**(anon 명시 REVOKE, `aclexplode` 검증 완료). 특정 유저 관점의 finished 매치 리스트 반환(match_id/result(win·loss·draw)/problem_title/opponent_id/opponent_nickname/opponent_avatar_url/my_mmr_change/end_time). 상대 조인 `LEFT JOIN LATERAL ... LIMIT 1`로 매치당 1행 보장. 반환 PII는 공개 프로필(닉네임/아바타)뿐, 비밀 토큰 제외. 대전 히스토리 표시용 (2026-06-13, Post-MVP A-5)
  - `cleanup_stale_matches_and_queue()` — SECURITY DEFINER, **service_role EXECUTE 단독**(anon/authenticated 명시 REVOKE). pg_cron 15분 주기 좀비 정리(B-5/B-7): stale ongoing(제출有→finished 몰수/제출無→DELETE) + 만료 waiting→DELETE + queue 15min→DELETE, 모든 DELETE에 `NOT EXISTS(submissions)` 가드. 반환 `(finalized, deleted_ongoing, deleted_waiting, deleted_queue)`. **DB 적용·검증 완료(aclexplode anon/authenticated 부재 + cron job active 확인)** (2026-06-14, Post-MVP B-5/B-7)
- `auth.users` AFTER INSERT 트리거: `handle_new_user` 적용 완료
- `matches`: `invite_token` UNIQUE + `invite_expires_at` + `host_id` 컬럼 (PR #6 선반영, PR #7-D/#14에서 사용)
- `supabase_realtime` publication: `matches` + `match_participants` + **`matchmaking_queue`(MVP A-2 — 대기자가 자기 큐 row `match_id` UPDATE 를 `useMatchmakingQueue` postgres_changes 로 감지)** 포함 (PR #7-D `useMatchStatus`)
- 시드: `problems` 9건 + `test_cases` 43건 (visible 27 / hidden 16) — 멱등 마이그레이션 SoT 확보 (PR #8)

## 마지막 갱신

- **2026-06-14** — **좀비 자동 정리 (Post-MVP B-5/B-7) — 화면 변경 없음(DB 전용)**. `cleanup_stale_matches_and_queue()` SECURITY DEFINER 함수 + pg_cron 15분 주기 등록(마이그레이션 2개 분리: function/schedule). stale ongoing(제출有→finished 몰수/제출無→DELETE)·만료 waiting·queue 15min 정리. brainstorming→spec→plan + code-reviewer(opus) Critical 1건(트랜잭션 경계→파일 분리) 반영. **DB 적용·검증 완료(사용자 직접)**. 화면 라우트/컴포넌트 영향 0.
- **2026-06-13** — **대전 히스토리 (Post-MVP A-5)**. `/profile/[userId]` 누적 전적 아래에 개별 매치 리스트 섹션(`MatchHistorySection`) 추가 — 승/패/무 배지 + 상대 아바타/닉네임 + 문제 제목 + 날짜(`formatJoinDate` 재사용) + MMR변동(NULL 숨김), 행 전체 클릭→`/result/[matchId]`. `get_match_history(p_user_id, p_limit)` SECURITY DEFINER RPC(RLS 우회, authenticated 단독·anon REVOKE `aclexplode` 검증, 상대 조인 LATERAL LIMIT 1) + `getMatchHistory` fetch 래퍼(빈 배열 fallback) + `IMatchHistoryEntry` 타입 + `isAnonymousNickname` 유틸(Anon\_ 전용). 본인·타인 프로필 모두, 익명 상대는 "익명 상대" 라벨, 최근 20건. 데이터는 기존 matches/match_participants에 존재 — 새 테이블/컬럼 없음. agent-team-workflow(opus, Critical 0, W-1 LATERAL 반영). DB 적용·권한·로직 검증 완료. 리뷰 nitpick 2건(MMR 0 표기·row 타입) 반영. **PR #31 dev squash 머지 완료(`cfc1405`)**.
- **2026-06-13** — **리더보드 전적 표시 (Post-MVP A-1 후속)**. `/leaderboard` 각 행에 닉네임 아래 `7승 4패 · 64%` 서브텍스트(승/패/무 + 승률) 추가. `get_leaderboard` SECURITY DEFINER RPC 신설(전체 유저 mmr+전적 집계, authenticated 단독, anon REVOKE `aclexplode` 검증 완료)로 `getLeaderboard`를 직접 select→RPC 전환. 승률 = wins/total_finished(무승부 포함 분모), 0전이면 "전적 없음". agent-team-workflow(opus, Critical 0). DB 적용 완료(사용자 직접). 미커밋.
- **2026-06-07** — PR #29(Post-MVP 전 소소 이슈) 머지. **화면 기능 변경 없음** — 코드 품질 전용(status 상수화 / Realtime 훅 개선 / `app/api/ai` 제거). 유일한 시각 변경: `/login`의 보조 텍스트 색을 `text-zinc-400`→`text-muted-foreground` 디자인 토큰으로 통일(명도 거의 동일, 회귀 없음).
- **2026-06-06** — 문서 최적화 세션(이 문서의 마지막 갱신 이력 압축). 화면 변경 없음.
- **직전 화면 변경**:
  - **PR #26** 글로벌 네비 메뉴바(`GlobalNav` 홈/대전하기/리더보드, `usePathname` active 강조)를 `(main)/layout.tsx` + `HomeClient` 헤더 양쪽 마운트. 홈 "매치 찾기" 카드 제거(자동 매칭과 중복) → 카드 2개.
  - **PR #25** ProblemPanel 섹션 카드 — `/play` 좌측 문제 패널을 구조화 컬럼(description/input_format/output_format/examples) 기반 `ProblemMetaHeader`/`ProblemSection`/`ProblemExampleSection`로 재구성.
- MVP 핵심 화면 루프(`/` → 매칭 → `/play` → `/result` → `/leaderboard` → `/profile`)는 전부 ✅.
- 세션별 상세 작업 이력 SoT는 `docs/NEXT_SESSION.md` + git 히스토리.
